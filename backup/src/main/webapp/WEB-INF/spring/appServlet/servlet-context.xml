<beans:beans xmlns="http://www.springframework.org/schema/mvc" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xsi:schemaLocation=" http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">
    <!--
    DispatcherServlet Context: determines the settings of a servlet, beans that are only available to him
    -->
    <!--
    Allow the use of annotations Spring MVC (@Controller etc.)
    -->
    <annotation-driven/>
    <!--
    The entire static (images, css-files, javascript) put it in the webapp / resources and their zamappim URL for the form / resources / **
    -->
    <resources mapping="/resources/**" location="/resources/"/>
    <!--
    Display types for jsp-files which lie in the folder / WEB-INF/views
    -->
    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/"/>
        <beans:property name="suffix" value=".jsp"/>
    </beans:bean>
    <!-- Controllers configuration file -->
    <beans:import resource="controllers.xml"/>
</beans:beans>